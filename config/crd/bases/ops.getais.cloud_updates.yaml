---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: updates.ops.getais.cloud
spec:
  group: ops.getais.cloud
  names:
    kind: Update
    listKind: UpdateList
    plural: updates
    singular: update
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.versioning.sources[0].type
      name: Type
      type: string
    - jsonPath: .spec.versioning.sources[0].version
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.syncTimestamp
      name: Synced
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Update is the Schema for the updates API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              versioning:
                properties:
                  sources:
                    items:
                      properties:
                        name:
                          type: string
                        source:
                          type: string
                        type:
                          type: string
                        version:
                          type: string
                      required:
                      - name
                      - source
                      - type
                      - version
                      type: object
                    type: array
                required:
                - sources
                type: object
            required:
            - versioning
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    name:
                      type: string
                    source:
                      type: string
                    type:
                      type: string
                    version:
                      type: string
                  required:
                  - name
                  - source
                  - type
                  - version
                  type: object
                type: array
              phase:
                type: string
              syncTimestamp:
                type: string
            required:
            - conditions
            - phase
            - syncTimestamp
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
